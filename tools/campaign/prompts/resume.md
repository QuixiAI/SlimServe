# Resuming

Your previous session in this phase ended without an accepted marker.
{{FEEDBACK}}

Recover state from the repo, not from memory: the top section of HANDOFF.md
(your status log), the newest `perf/optimization_status.md` entries,
`git log --oneline {{BASE}}..HEAD`, `git status`, and `perf/results/` for
runs in flight. Do not re-research and do not re-measure what is already
pinned. Continue from the last recorded step.
